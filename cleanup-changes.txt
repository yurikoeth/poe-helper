POE-HELPER CODE CLEANUP — 8-PASS SUMMARY
==========================================

Passes were run sequentially. Typecheck (`pnpm run typecheck`) stayed green
throughout. Nothing was committed; all changes are in the working tree on top
of pre-existing uncommitted work.


PASS 1 — DEDUPLICATE & DRY
--------------------------
Consolidated poe.ninja URL construction into packages/shared/src/api/poe-ninja.ts:
  - Added buildNinjaUrl(game, league, category)
  - Exported NINJA_BASE_URLS and NINJA_CURRENCY_CATEGORIES
Removed duplicated NINJA_URLS map, CURRENCY_TYPES set, and URL logic from:
  - apps/overlay/src/hooks/usePriceCheck.ts
  - apps/overlay/src/components/MarketTab.tsx
Side effect: fixed latent bug where usePriceCheck hit poe.ninja for both
games instead of poe2.ninja for PoE2.

Intentionally NOT consolidated:
  - parseAiJson (single caller)
  - getStore/getApiKey (overlay-only, no shared counterpart)
  - format helpers (already in shared)
  - Per-component AI invoke wrappers (different request shapes)


PASS 2 — TYPE CONSOLIDATION
---------------------------
Promoted Rust IPC contract types to packages/shared/src/types/session.ts:
  - LogEventPayload  (log-event Tauri event)
  - InitialGameState (get_initial_game_state command)
useClientLog.ts now imports these from @exiled-orb/shared.

Left separate on purpose: local-only types in components (GggItem, NinjaItem,
build/speedrun store shapes, etc.). Different-shape LogEventPayload aliases
in useTradeWhispers/useFlipTracker were NOT merged (genuinely different
payloads).


PASS 3 — UNUSED CODE REMOVAL (knip + grep-verified)
---------------------------------------------------
Deleted:
  - apps/overlay/src/components/Settings.tsx (entire file, zero importers)
  - saveApiKey export in apps/overlay/src/utils/store.ts
  - getRunCount export in apps/overlay/src/stores/speedrun-db.ts
  - @tauri-apps/plugin-shell dep from apps/overlay/package.json

Adjusted: ActiveBuild in build-store.ts lost its unused `export`.

Verified live (kept): useTradeWhispers and useFlipTracker are called from
App.tsx — not dead.


PASS 4 — CIRCULAR DEPENDENCIES (madge)
--------------------------------------
Source scan: 80 files, 0 circular dependencies.
Initial madge false-positive came from traversing compiled .d.ts in
packages/shared/dist. No fixes required.


PASS 5 — WEAK TYPES
-------------------
Only one offender in the whole codebase:
  - apps/overlay/src/hooks/usePriceCheck.ts: ninjaFetch returned Promise<any[]>
    → Promise<NinjaLine[]> (new interface modeling poe.ninja line shape).

Rust src-tauri scanned: no Box<dyn Any> or over-broad generics.


PASS 6 — DEFENSIVE TRY/CATCH
----------------------------
Removed 5 error-swallowing wrappers:
  - useClipboard.ts:52            analyzeItemWithAi(...).catch(() => {})
  - speedrun-store.ts:186         saveMapRun(...).catch(() => {})
  - speedrun-store.ts:213         saveMapRun(...).catch(() => {})
  - speedrun-store.ts:263         deleteMapRun(...).catch(() => {})
  - speedrun-store.ts:291         deleteMapRun(...).catch(() => {})
  - MapSplitDisplay.tsx:42-50     outer try/catch around new Audio()

All were double-swallowing errors already handled inside the callees.

Kept (legitimate boundaries): tauri-plugin-store + SQLite wrappers, Tauri
invokes to failable Rust commands (AI, poe.ninja, GGG), parseAiJson
(untrusted AI output), ggg-trade external API, Rust oauth.rs parallel
realm fetches, log_watcher file I/O.


PASS 7 — DEPRECATED / LEGACY CODE
---------------------------------
Removed:
  - MOD_TIER_DB legacy export from packages/shared/src/data/mod-tiers.ts
  - Its re-export from packages/shared/src/data/index.ts

Everything else tagged "fallback" in comments was inspected and confirmed
to be live logic, not legacy.


PASS 8 — COMMENTS & AI SLOP
---------------------------
No edits. Scanned all 45+ TS/TSX/Rust files for:
  narration ("Now we", "Updated", "Replaced", "Previously"),
  TODO/FIXME/XXX/HACK, stubs/placeholders, redundant JSDoc,
  multi-paragraph docstrings.
None found. Remaining comments are PoE domain knowledge, public API
one-liners, section markers in long data files, or required Tauri
boilerplate.


FINAL DIFF (git diff --stat HEAD, includes your prior uncommitted work)
-----------------------------------------------------------------------
28 files changed, 838 insertions(+), 666 deletions(-)
